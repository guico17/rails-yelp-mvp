<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>

<%= link_to 'Back to reviews', restaurant_reviews_path(@restaurant) %>
<% end %>
